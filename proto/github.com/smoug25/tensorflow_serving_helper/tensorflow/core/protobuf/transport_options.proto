syntax = "proto3";

package tensorflow;
option go_package = "github.com/smoug25/tensorflow_serving_helper/tensorflow/core/protobuf";

// Extra data needed on a non-RDMA RecvBufResponse.
message RecvBufRespExtra {
  repeated bytes tensor_content = 1;
};
